import { createBase2 } from './base2'

const definition = {
  ...createBase2('default', { planOnly: true }),
  id: 'base2-plan',
  displayName: 'Nevan Code Plan-Only Orchestrator',
}
export default definition
